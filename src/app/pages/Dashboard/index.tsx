import { Box } from '@mui/material';

const DashboardPage = () => {
  return (
    <Box
      sx={{
        backgroundColor: 'var(--blue4)',
        height: '100%',
        width: '100%',
      }}
    >
      <Box>
        <h1>Dashboard</h1>
      </Box>
    </Box>
  );
};

export default DashboardPage;
