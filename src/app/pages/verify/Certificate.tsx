import { Box, Typography } from '@mui/material'
import { useEffect } from 'react'

export const CertVerifyPage = (props) => {
  const {
    state = {
      data: [],
      matched: '',
    },
  } = props

  if (state.data === 0) return <></>

  return<Box sx={{ width: '90%' }}>
  ``

  </Box>
}
